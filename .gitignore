__pycache__/
*.pyc
.venv/
venv/
.env
.DS_Store
*.log
.pytest_cache/
site/

# Earnings-AI (app/eai) local artifacts
eai.db
eai.db-journal
eai_storage/
_alembic_probe.db
# Harvested raw transcripts are NOT redistributed (licensed content): keep them
# out of the public repo. Only the ledger (harvest_state.json) + derived
# analysis snapshot are committed. The drop-folder README is kept.
data/eai/transcripts/*.json
data/eai/transcripts/*.txt
# Frontend build/deps (also covered by frontend/.gitignore)
frontend/node_modules/
frontend/.next/
frontend/next-env.d.ts

# Market Regime Lab price cache (재다운로드 가능, 커밋하지 않음)
data/regime/
