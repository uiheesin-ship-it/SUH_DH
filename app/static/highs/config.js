// Run mode flag. This file is overwritten by build.py for the static
// GitHub Pages build (which sets SUH_DH_STATIC=true). Locally it stays false
// so the dashboard talks to the live FastAPI backend.
window.SUH_DH_STATIC = false;
