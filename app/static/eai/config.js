// Run mode flag (overwritten by build.py for the static GitHub Pages build,
// which sets SUH_DH_STATIC=true). Locally it stays false so the page talks to
// the live FastAPI backend at /api/eai/*.
window.SUH_DH_STATIC = false;
