// Run mode flag. build.py overwrites this for the static GitHub Pages build
// (sets SUH_DH_STATIC=true). Locally it stays false so the page talks to the
// live FastAPI backend (/api/base).
window.SUH_DH_STATIC = false;
