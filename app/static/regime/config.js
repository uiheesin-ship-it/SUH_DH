// Run mode flag + Regime Lab 서버 주소.
// build.py 가 정적 빌드(GitHub Pages)에서 두 값을 모두 덮어씁니다.
// 로컬에서는 STATIC=false, URL="" 이라 같은 서버의 백엔드(/api/regime/*)를 씁니다.
window.SUH_DH_STATIC = false;
window.SUH_DH_REGIME_URL = "";
